/* ===========================================================
   ResQ Tyres — reg → tyre-size lookup: REMOVED (2026-07-01)
   The client decided not to use the DVLA/reg tyre-size finder.
   Customers now enter their tyre size manually. This file is a
   no-op left in place only because the folder mount blocks file
   deletion; it is no longer referenced by index.html and can be
   deleted in Finder along with /api/tyre-lookup.js.
   =========================================================== */
